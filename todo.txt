-> Para todos los grupos, la parte de seleccionar productos, hay que optimizarla, de alguna manera, ahora mismo tiene un timeout, que hace que vaya lento, solucionar de la manera más óptima.

-> Arreglar también el precio que sale, cuando es un producto con controlador, ya que es precio base.

-> Ver porque cuando se selecciona una combinación de un producto, porque se mete directamente a la funcion updateBar()

-> Arreglar el tpl del controlador, botones más grandes, nombres completos, tal vez añadir funcionalidad como en prestashop de poder añadir 18 a la vez...

-> Poner un modal, cuando se realiza el pedido indicando si se ha accedido a la promoción y a cual

-> A parte de una optimización y refactorización general

-----------------------------------------------------------
-> Meterle una verificación para que no se renderice, lo del navegador si ya has accedido al grupo ?